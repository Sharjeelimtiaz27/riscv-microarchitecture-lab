# Claude Code session files — local only, not for version control
CLAUDE.md
napkin.md

# Simulation artifacts
xcelium.d/
work/
artifacts/
waves.vcd
*.vcd
*.shm/
*.diag

# EDA tool scratch -- large, regenerable, tool-internal (do NOT commit)
# JasperGold session scratch: everything under a session dir except the .gitkeep
# (jg -proj writes jg.log, jg.cmd, sessionLogs/, work.bak/, .tmp/ directly here)
formal/sessions/*/*
!formal/sessions/*/.gitkeep
jgproject/
# Cadence runtime timestamp
.rs_*.tstamp
# Genus formal-verification scratch and run logs
fv/
genus.log*
genus.cmd*
.genus*
# Innovus run logs
innovus.log*
innovus.cmd*
*.rpt.gz
# Backup files
*.bak
# cocotb / pyuvm simulation artifacts
sim_build/
results.xml
xrun.log
xrun.history
xrun.key

# Python
__pycache__/
*.pyc
*.pyo

# OS
.DS_Store
Thumbs.db

# =============================================================================
# CONFIDENTIAL / COMMERCIAL-LICENSE (NDA) -- NEVER COMMIT
# The local PDK config holds foundry/vendor paths and library names.
pdk_local.tcl
# Foundry-mapped artifacts embed licensed cell names and characterized timing
# (gate-level netlists, SDF delays, area/timing/power reports). They are
# regenerable on the HPC from RTL; keep them out of the public repo.
syn/results/*
!syn/results/.gitkeep
pnr/results/*
!pnr/results/.gitkeep

# Claude Code local session dir
.claude/
